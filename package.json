{
  "name": "time-tracker",
  "version": "1.0.0",
  "description": "time tracker app",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV=dev electron main.js",
    "build": "NODE_ENV=prod electron builder"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "devtron": "^1.4.0",
    "electron": "^4.1.0",
    "electron-reload": "^1.4.0"
  },
  "dependencies": {
    "bcrypt": "^3.0.5",
    "nedb": "^1.8.0",
    "nedb-promises": "^3.0.2"
  }
}
